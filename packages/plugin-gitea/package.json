{
  "name": "@car/plugin-gitea",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "types": "./src/index.ts",
    "default": "./dist/index.js"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "check": "tsc --noEmit",
    "clean": "tsc -b tsconfig.build.json --clean",
    "test": "pnpm run build && node --test test/*.test.js"
  },
  "dependencies": {
    "@car/core": "workspace:*"
  }
}
